import React from "react";
import MainTemplate from "../../template/main-template/main";

const Video = (props)=>{

    return(
        <MainTemplate>

        </MainTemplate>
    )
}

export default Video;